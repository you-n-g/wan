# Get the api of the notifiers

## Telegram
* get `token` from `@BotFather` by sending `/newbot`
* send `/start` to your chatbot to start chatting.
* Get `chat_id` from  `@myidbot` by sending `/getid`
